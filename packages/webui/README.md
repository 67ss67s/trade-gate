# trade-gate Web UI

React / shadcn / react-query 前端，通过 gateway API 展示交易线程、意图审批和工作流设置。自动仓位在 workflow form 中选择关/只建议/采用，审批意图卡展示代码 sizing 与 agent 理由；不计算或提交模型生成的数量。

类型检查：在此目录运行 `npx tsc --noEmit -p .`。契约见 `design notes`。
